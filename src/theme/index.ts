import { createTheme, colors } from "@material-ui/core";

export const theme = createTheme({
    palette: {
        primary: {
            main: colors.red[800]
        },
        secondary: {
            main: colors.blue[500]
        }
    },
    /*typography: {
        
    }*/
});